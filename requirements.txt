# Dots.OCR 推理客户端依赖
# 用于连接已部署的vLLM服务进行文档解析
# Python版本要求: >=3.10
# 此文件保留 v0.2 的完整非 GPU 安装语义；新部署优先使用
# `pip install 'ocrparser-platform[full]'`，基础 Parser 使用默认安装。

# 核心依赖
# pillow-simd==9.5.0.post2 # 图像处理
pillow
PyMuPDF==1.26.3          # PDF处理
openai==1.90.0           # vLLM API客户端
python-dotenv            # 读取数据索引 .env 配置
pydantic>=2.0.0,<3.0     # PageInfo 数据结构
tqdm==4.67.1             # 进度条
opencv-python==4.12.0.88 # 计算机视觉处理
numpy==2.1.2             # 数值计算
requests==2.32.4         # HTTP请求
json_repair==0.51.0      # JSON格式修复
pyzbar==0.1.9            # 条形码和二维码识别
ImageHash==4.3.2        # 图像哈希计算
aiofiles
aiobotocore==2.13.0     # 异步 S3 下载
botocore>=1.34.0,<1.35.0 # aiobotocore 兼容版本

# 异步处理
aiohttp==3.12.15         # 异步HTTP客户端
httpx==0.28.1            # HTTP客户端（用于OpenAI）
idna                     # 国际化域名支持 (requests的依赖)
prometheus_client        # Prometheus metrics HTTP server
PyYAML                  # YAML engine_config files
fastapi
uvicorn[standard]
SQLAlchemy>=2.0,<3.0
psycopg[binary]>=3.1,<4.0
