# Public synthetic PDF fixtures

These PDFs contain generated benchmark text only. They were produced from
`tools/generate_benchmark_pdfs.py`; they contain no customer documents, internal
endpoints, or private data. Keep them small so contract and mock OCR tests remain
fast.
